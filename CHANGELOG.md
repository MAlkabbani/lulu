# Changelog

All notable project-level changes should be tracked here.

## Unreleased

- hardened runtime startup cleanup so failed bootstrap still closes UI and TTS resources
- replaced shell `source`-based `.env` loading in scripts with strict key/value parsing
- moved development-only tooling into `requirements-dev.txt` and added `pyproject.toml`
- added architecture, roadmap, contribution, security, and community documentation
- refreshed repository metadata for open-source readiness
